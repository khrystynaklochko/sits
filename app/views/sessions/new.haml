%h1 Log in

= simple_form_for :session, :url => sessions_path do |f|
  = f.error_notification

  .form-inputs
    = f.input :email
    = f.input :password

  .form-actions
    = f.button :submit, 'log in'